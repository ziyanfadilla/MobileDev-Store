<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.utsmobile">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UtsMobile">
        <activity android:name="ziyan.fadila.utsmobile.UpdateActivity"></activity>
        <activity android:name="ziyan.fadila.utsmobile.CreateBarangActivity" />
        <activity android:name="ziyan.fadila.utsmobile.DetailActivity" />
        <activity android:name="ziyan.fadila.utsmobile.BarangActivity" />
        <activity android:name="ziyan.fadila.utsmobile.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="ziyan.fadila.utsmobile.MainActivity" />
    </application>

</manifest>